import React from 'react';
import styled from 'styled-components';

const STemplate = styled.header`

`;

const Template: React.FC = () => {
  return (
    <STemplate>
    </STemplate>
  );
}

export default Template;
